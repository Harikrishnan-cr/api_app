



import 'package:flutter/material.dart';
class ExamScreenMyApp extends StatelessWidget {
  const ExamScreenMyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

      body: Column(
        children: [
          Expanded(child: Text('data is')),
          Icon(Icons.abc),
          
        ],
      ),
    );
  }
}